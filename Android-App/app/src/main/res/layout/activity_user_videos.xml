<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/user_videos_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_margin="16dp"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/user_videos_icon"
            android:layout_width="37dp"
            android:layout_height="match_parent"
            android:contentDescription="@string/user_videos_icon_text"
            android:src="@drawable/ic_user_videos" />

        <TextView
            android:id="@+id/user_videos_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/user_s_videos"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/colorText" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/user_videos_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/user_videos_header"
        android:background="@color/colorBackground" />

</RelativeLayout>
